#+begin_src jupyter-python
import proplot as plt, cmasher as cmr, pandas as pd,\
    numpy as np, os, sys, networkx as nx, warnings
from plexsim import models
from imi import infcy

g = nx.krackhardt_kite_graph()
mu = 0
eta = 1
m = models.Pottsis(g, beta = 1, mu = mu, eta = eta, sampleSize = 1, seed = 0)


s = m.simulate(10000)
fig, ax = plt.subplots()
ax.plot(s.mean(1))
ax.format()
fig.show()
#+end_src

#+RESULTS:
:RESULTS:
: /tmp/ipykernel_180826/444236945.py:16: UserWarning: Matplotlib is currently using module://matplotlib_inline.backend_inline, which is a non-GUI backend, so cannot show the figure.
:   fig.show()
: 59049
#+attr_org: :width 288 :height 282
[[file:./.ob-jupyter/a00f92f374d89ddb7780dfd681aa3934d7a88d7a.png]]
:END:
